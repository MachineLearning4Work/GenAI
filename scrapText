


from pydantic import BaseModel, Field
from typing import Optional

class ServiceLocation(BaseModel):
    service_id: str = Field(..., description="Unique identifier for the service")
    location_index: int = Field(..., description="Incremental index per location")
    city: str = Field(..., description="City name")
    state: str = Field(..., description="Province or state name")
    country: str = Field(..., description="Official country name")
    continent: str = Field(..., description="Continent name")
    full_address: Optional[str] = Field(None, description="Full address if available")
    original: str = Field(..., description="Original input string")
